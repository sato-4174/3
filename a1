body {
    background-color: #3175b5;
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

.header {
    background-color: #333; /* ヘッダーの背景色 */
    color: #fff;
    padding: 20px;
    text-align: center;
    font-size: 24px;
}

.grid-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr); /* 横に4列のグリッド */
    grid-gap: 20px; /* グリッド間の隙間 */
    padding: 50px;
    max-width: 1200px; /* コンテナの最大幅を1200pxに設定 */
    margin: 0 auto; /* 左右中央揃え */
}

.box {
    display: flex;
    justify-content: center;
    align-items: center;
    overflow: hidden;
    margin-bottom: 20px; /* 画像間の余白 */
    width: 250px; /* 画像の幅を250pxに設定 */
    height: 250px; /* 画像の高さを250pxに設定 */
}

.box img {
    width: 100%; /* ボックスの幅に合わせる */
    height: auto; /* 高さを自動で調整し、アスペクト比を保持 */
    object-fit: cover; /* 画像をトリミングしてボックスに合わせる */
}

body {
    margin: 0;
    font-family: Arial, sans-serif;
    background: #f4f4f4;
}

.sidebar {
    width: 250px;
    background: #fff;
    height: 100vh; /* Full height of the viewport */
    box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
    position: fixed;
    left: 0;
    top: 0;
    overflow-y: auto;
    padding: 20px;
}

.sidebar input[type="search"] {
    width: 100%;
    padding: 10px;
    margin-bottom: 20px;
    border: 1px solid #ddd;
    border-radius: 20px;
}

.menu {
    list-style-type: none;
    padding: 0;
}

.menu-item {
    padding: 10px 15px;
    margin-bottom: 5px;
    cursor: pointer;
    color: #333;
}

.menu-item:hover {
    background: #f0f0f0;
}

.content {
    margin-left: 250px; /* Same as sidebar width */
    padding: 20px;
}

.sidebar input[type="search"] {
    width: 100%; /* 検索バーの幅をサイドバーの幅いっぱいに設定 */
    padding: 10px; /* 内側の余白を設定 */
    margin: 0 0 20px 0; /* 下部の余白のみを設定 */
    border: 1px solid #ddd;
    border-radius: 20px;
    box-sizing: border-box; /* ボーダーとパディングを幅に含める */
}

.grid-container {
    display: grid;
    grid-template-columns: repeat(4, 1fr); /* 3列のグリッド */
    grid-gap: 20px; /* グリッド間のスペース */
    padding: 50px;
    max-width: 1200px; /* コンテナの最大幅 */
    margin-left: 115px; /* 左のマージンを減らして、コンテンツを左により寄せる */
    margin-right: auto; /* 右のマージンを自動調整 */
    margin-top: 0;
    margin-bottom: 0;
    height: 100%; /* コンテンツの全高さ */
    box-sizing: border-box; /* ボックスサイズの計算方法を指定 */
}


body, html {
    margin: 0;
    padding: 0;
    max-width: 100%; /* Ensure the body does not exceed the width of the viewport */
    box-sizing: border-box;
}

.wrapper {
    max-width: 800px; /* Adjust this value to match the width of the ChatGPT window as seen in your screenshot */
    margin: 0 auto; /* This will center your wrapper within the viewport */
}

/* The rest of your styles can remain the same, ensure to wrap your content within the .wrapper div */

/* Example */
/*
<div class="wrapper">
    <!-- All your content goes here -->
</div>
*/

body, html {
    margin: 0;
    padding: 0;
    font-family: 'Hiragino Kaku Gothic ProN', 'Meiryo', sans-serif;
    background: #f4f4f4;
}

.app-container {
    display: flex;
    min-height: 100vh;
}

.sidebar {
    width: 260px;
    background-color: #202020;
    padding: 20px;
    box-sizing: border-box;
}

.search-container {
    padding: 10px;
    background-color: #333;
    border-radius: 30px;
    margin-bottom: 30px;
}

input[type="search"] {
    width: 100%;
    background-color: #333;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    color: #fff;
    border-radius: 30px;
}

input[type="search"]::placeholder {
    color: #555;
}

input[type="search"]:focus {
    outline: none;
    background-color: #444;
}

/* その他のスタイル定義 */
.content {
    flex: 1;
    padding: 20px;
    background-color: #fff;
    /* その他のコンテンツエリアのスタイリング */
}

/* サイドバーのメニュー項目のスタイル */
/* ... */

body, html {
    margin: 0;
    padding: 0;
    font-family: 'Hiragino Kaku Gothic ProN', 'Meiryo', sans-serif;
    background: #f4f4f4;
}

.app-container {
    display: flex;
    min-height: 100vh;
}

.sidebar {
    width: 260px;
    background-color: #2D2D2D;
    padding: 20px;
    box-sizing: border-box;
}

.logo-container h1 {
    color: #FFFFFF;
    text-align: center;
    padding-bottom: 20px;
}

.search-container {
    padding: 10px 15px;
    background-color: #373737;
    border-radius: 30px;
    margin-bottom: 30px;
}

input[type="search"] {
    width: 100%;
    background-color: transparent;
    border: none;
    padding: 10px 20px;
    font-size: 16px;
    color: #FFFFFF;
    border-radius: 30px;
}

input[type="search"]::placeholder {
    color: #BBBBBB;
}

input[type="search"]:focus {
    outline: none;
}

.navigation-menu {
    display: flex;
    flex-direction: column;
}

.nav-item {
    color: #BBBBBB;
    padding: 10px 0;
    text-decoration: none;
    font-size: 16px;
    transition: color 0.3s ease;
}

.nav-item:hover, .nav-item.active {
    color: #FFFFFF;
}

.content {
    flex-grow: 1;
    padding: 20px;
    background-color: #fff;
    overflow-y: auto;
}


body {
    margin: 0;
    padding: 0;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background: #f4f4f4;
    color: #333;
}

.sidebar {
    background-color: #ddd;
    padding: 20px;
    width: 250px;
    height: 100vh;
    box-shadow: 2px 0 10px rgba(0, 0, 0, 0.1);
    position: fixed;
    display: flex;
    flex-direction: column;
}

.branding {
    font-size: 1.5em;
    margin-bottom: 1em;
    text-align: center;
}

.search-box {
    display: flex;
    flex-direction: column;
}

.search-box input {
    padding: 10px;
    margin-bottom: 10px;
    border: none;
    border-radius: 4px;
}

.search-box button {
    padding: 10px;
    background-color: #333;
    color: white;
    border: none;
    border-radius: 4px;
    cursor: pointer;
}

.search-results {
    margin-top: 20px;
    overflow-y: auto;
}

.search-result-item {
    background: #fff;
    margin-bottom: 10px;
    padding: 10px;
    border-radius: 4px;
    cursor: pointer;
}

.search-result-item:hover {
    background: #e9e9e9
;
}

/* メディアクエリでモバイルビュー対応 */
@media (max-width: 768px) {
.sidebar {
width: 100%;
height: auto;
box-shadow: none;
}
}



